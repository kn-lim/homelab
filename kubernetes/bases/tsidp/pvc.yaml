apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: tsidp-data
  namespace: tsidp

spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: nfs-data
  resources:
    requests:
      storage: 10Gi
