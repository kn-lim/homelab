apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: jellyfin-media
  namespace: selfhosted

spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 25Gi
