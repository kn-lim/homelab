apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: jellyfin-cache
  namespace: selfhosted

spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
