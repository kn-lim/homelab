apiVersion: builtin
kind: HelmChartInflationGenerator

metadata:
  name: traefik

valuesInline:
  service:
    annotations:
      lbipam.cilium.io/ips: "10.1.2.21"

  ports:
    traefik:
      expose:
        default: true
