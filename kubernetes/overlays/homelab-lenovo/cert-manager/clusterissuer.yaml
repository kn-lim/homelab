apiVersion: cert-manager.io/v1
kind: ClusterIssuer

metadata:
  name: letsencrypt

spec:
  acme:
    # https://letsencrypt.org/docs/acme-protocol-updates/#api-endpoints
    server: https://acme-v02.api.letsencrypt.org/directory
    privateKeySecretRef:
      name: letsencrypt-account-key
    solvers:
      - dns01:
          cloudflare:
            apiTokenSecretRef:
              name: cloudflare-knlim.dev
              key: credential
