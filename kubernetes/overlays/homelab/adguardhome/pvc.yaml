apiVersion: v1
kind: PersistentVolumeClaim

metadata:
  name: adguardhome-data
  namespace: adguardhome

spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-data
  resources:
    requests:
      storage: 2Gi
