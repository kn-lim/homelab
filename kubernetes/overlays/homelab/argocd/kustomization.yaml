apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generators:
  - helm-values

resources:
  - ../../../bases/argocd
  - application.yaml
  - applicationset.yaml
  - argocd-rbac-cm.yaml
  - ingress.yaml
  - namespace.yaml
  - ssh-secret.yaml
  - tsidp-secret.yaml
