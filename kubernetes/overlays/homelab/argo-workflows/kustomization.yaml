apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

generators:
  - helm-values

resources:
  # - ../../../bases/argo-workflows
  - environment-secret.yaml
  - ingress.yaml
  - serviceaccount.yaml
  - ssh-secret.yaml
