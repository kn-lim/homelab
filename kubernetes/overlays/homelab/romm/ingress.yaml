apiVersion: networking.k8s.io/v1
kind: Ingress

metadata:
  name: romm
  namespace: romm

spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: romm
      port:
        number: 8080
  tls:
    - hosts:
        - romm
